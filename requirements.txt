# This file is managed by pyproject.toml.
# Install with:  uv sync  (or  uv pip install -e ".[flash-attn]")
# To add a dep, run:  uv add <package>   (updates both pyproject.toml and this file)
#
# NOTE: PyTorch is pulled from the pytorch-cu124 index (see pyproject.toml [tool.uv.sources]).
#       If you don't use uv, install torch first:
#         pip install torch==2.4.1 torchvision==0.19.1 torchaudio==2.4.1 --index-url https://download.pytorch.org/whl/cu124
#       Then: pip install -e ".[flash-attn]"

--extra-index-url https://download.pytorch.org/whl/cu124

torch>=2.4.0
torchvision
torchaudio
transformers>=4.45.0
accelerate>=0.34.0
peft>=0.13.0
trl>=0.11.0
deepspeed>=0.15.0
bitsandbytes>=0.44.0
qwen-vl-utils
liger-kernel>=0.8.0
ujson
tensorboard
wandb
einops
sentencepiece
protobuf
math-verify
latex2sympy2-extended
gradio
pillow
datasets
# flash-attn is optional because it is a C++ extension.
# Install it separately with: uv pip install flash-attn --no-build-isolation
# flash-attn>=2.6.0
